/* ============================================================
   BARBEARIA KLEVER — main.js
   Funções globais usadas em múltiplas páginas
   ============================================================ */

/* Abre/fecha o popup de avaliação por estrelas */
function togglePopup(id) {
  document.getElementById(id).classList.toggle('aberto');
}
